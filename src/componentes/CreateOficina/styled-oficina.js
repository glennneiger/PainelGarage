import styled from 'styled-components'

export const Oficina = styled.main`
    margin: 20px;
    padding: 40px;
    background-color: var(--color-cinza);
    input { margin: 0; margin-top: 5px; }
    .form-group { margin: 15px 0; }
    .hf { margin-left: 15px; }
    input[type=submit] {
        color: white;
    }
`